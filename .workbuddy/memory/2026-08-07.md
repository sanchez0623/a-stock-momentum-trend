# 2026-08-07 工作日志

## 一期「地基」实现完成(commit 8483a8e)

按《个人动量趋势交易系统-设计方案》落地一期,关键决策(用户确认):
- 按四期逐期交付;数据源按方案默认(mootdx 优先);GitHub 工程文件备好稍后托管;AI复盘接 DeepSeek(四期配置,现留空)
- 项目直接建在工作区根目录(backend/ frontend/ 平铺),本地 Python 用系统 3.11.9 与 Docker python:3.11-slim 一致
- LICENSE 以现状为准:Apache 2.0(方案写 MIT,但工作区已有 Apache 2.0 文件)

## 交付内容
- 数据源层: DataSourceManager(健康分/熔断3次10min/近期偏好/自动切换) + mootdx/腾讯/东财(降频≥2s+串行+退避+UA+NID)/AKShare;K线 SQLite 缓存 + 实时 LRU(5s)
- 指标库纯函数: MA/EMA/MACD/RSI/ADX/ROC/布林/唐奇安/ATR/量比
- 配置中心 env>DB>默认;SQLite 全表(SQLModel);API: health/config/data-sources/quote/kline/indicators + WS
- 前端 Vite5+React18+TS 骨架(9 页面路由, build 通过)
- Dockerfile 多阶段 + compose 开发/生产 + CI(GHCR);25 项单测全绿(ruff 通过)

## 踩坑记录(重要)
1. **mootdx 默认 bestip 探测已失效**: 内置服务器列表大面积不可达,必须直连指定服务器(已内置 9 台经典服务器轮换,2026-08 实测全可用)。若再失效需更新 MOOTDX_SERVERS。
2. **8000 端口被占用**: 本机 8000 有未知服务(返回 {"status":"ok","provider":"akshare"}),疑似另一个 A股项目在跑。用户严禁改端口,已用 8001 临时验证,待用户处理 8000 冲突。
3. **系统代理环境变量**: 本机 HTTP_PROXY/HTTPS_PROXY=127.0.0.1:7897(科学上网),会污染 akshare(requests 走代理)。init_app 已清理代理 env + akshare 源内 _no_proxy_env 防御。东财对 akshare 有连接级风控(RemoteDisconnected),akshare 仅作末位兜底。
4. **pip 偶发静默失败**: Windows 上 pip install 显示 completed 但包未装上(留下 ~ttpx 残留),装完必须 import 验证。本次 httpx/mootdx/akshare 都曾出现。
5. **tsc -b 生成 vite.config.js/d.ts 产物**: build 脚本已改 tsc --noEmit;注意 Vite build 清空 dist 会被 WorkBuddy safe-delete 拦截,需先手动删 dist。
6. **Windows PowerShell 工具输出为空**: 查询进程/删文件时输出异常,改用 bash 绝对路径操作。

## 待办
- 8000 端口冲突需用户决策(停旧服务 or 确认归属)
- 二期: 信号引擎(五类信号)+ 仓位管理(金字塔/凯利)+ 风控三道闸 + 选股器 + 交易计划生成
