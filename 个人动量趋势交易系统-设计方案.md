# 个人动量趋势交易系统 — 设计方案

> 目标：一套本地运行的 A 股动量/趋势交易决策系统。**不接实盘下单**，只做「选股 → 信号 → 计划 → 指引 → 记录 → 复盘 → 评分」全链路。含加减仓与做 T，多数据源自动切换，AI 复盘可配 LLM。
>
> 技术栈遵循既有约定：后端 FastAPI（端口 **8002**）+ 前端 Vite（**5175**）。中文环境，涨红跌绿。**Docker 一键部署 + GitHub 托管**（CI/CD、GHCR 镜像发布）。

---

## 0. 一句话定位

> **给一个人的「交易副驾」**：自动盯盘 + 出策略 + 给人话操作指引 + 事后复盘打分，帮你在动量趋势框架下做更纪律化的决策，而不是替你按按钮。

---

## 1. 总体架构

分层 + 单向依赖，自下而上：

```
┌─────────────────────────────────────────────────────────────┐
│  前端层 (Vite + React, 5175)                                 │
│  仪表盘 / 选股 / 持仓 / 计划 / 日志 / 复盘 / 评分 / 设置       │
└──────────────────────────┬──────────────────────────────────┘
                           │ REST + WebSocket(行情推送,可选)
┌──────────────────────────┴──────────────────────────────────┐
│  API 层 (FastAPI, 8002)  —— 10 个路由分组, 见 §6              │
├─────────────────────────────────────────────────────────────┤
│  业务核心层                                                  │
│  选股器 · 信号引擎 · 仓位管理 · 风控 · 评分 · AI复盘 · 计划生成 │
├─────────────────────────────────────────────────────────────┤
│  基础设施层                                                  │
│  指标库 · 数据源管理器(多源切换) · 缓存 · 日志 · 配置中心        │
├─────────────────────────────────────────────────────────────┤
│  存储层  SQLite + CSV交易日志 + K线缓存                        │
└─────────────────────────────────────────────────────────────┘
```

**关键设计原则**

- **数据源与业务解耦**：业务只调 `DataSourceManager`，不直接碰任何上游 SDK，换源零成本。
- **纯计算无副作用**：信号/指标/评分/仓位都是纯函数，输入行情 → 输出决策，方便测试与回放。
- **决策与执行分离**：系统只产出「计划 + 指引」，人工执行后手动回填成交，再由评分/复盘闭环。
- **配置驱动**：阈值、权重、风控参数、LLM 配置全部进 `config`，热更新不改码。

---

## 2. 技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 后端框架 | FastAPI + Uvicorn | 异步、自带 OpenAPI、已有约定 |
| 异步任务 | APScheduler（定时选股/盯盘） + asyncio | 轻量，无需 Celery |
| 数据处理 | pandas + numpy | 指标计算主力 |
| 指标库 | 自实现（pandas）+ 可选 TA-Lib | 避免编译依赖，纯 pandas 先跑通 |
| 数据源 | mootdx + 腾讯财经 + 东财 push2his + AKShare(聚合) | 多源互备 |
| 数据库 | SQLite (sqlmodel/SQLAlchemy) | 单文件、零运维、个人系统足够 |
| 日志存储 | 结构化 CSV + SQLite 双写 | CSV 便于导出/Excel，SQLite 便于查询 |
| HTTP 客户端 | httpx（异步） | 东财/腾讯走它 |
| 终端可视化 | rich | 表格/进度条/面板 |
| 前端框架 | React 18 + TypeScript | 生态成熟、性能好 |
| 构建 | Vite 5 | 已有约定，HMR 快 |
| K线图 | lightweight-charts (TradingView) | 性能极佳，专为金融数据 |
| 图表 | ECharts（热力图/饼图/曲线） | 中文文档全、热力图支持好 |
| 状态管理 | Zustand | 轻量，比 Redux 省心 |
| 数据获取 | TanStack Query (React Query) | 缓存/重试/后台刷新内置 |
| 虚拟列表 | @tanstack/react-virtual | 处理数千只股票列表 |
| UI 组件 | 自建 + 少量 Radix UI 原语 | 控制体积，按需引 |
| 代码质量 | ruff + mypy + pytest | 快速 lint + 严格类型 + 可测 |
| 容器化 | Docker + docker-compose | 开发/生产双模式、数据卷持久化 |
| CI/CD | GitHub Actions | 自动测试构建，tag 自动发布 GHCR 镜像 |

**端口约定**：后端 `8002`（2026-08-07 调整：本机 8000 已被另一项目占用，经确认本项目改用 8002），前端 `5175`（2026-08-07 调整：本机 5173 已被其他项目占用，经确认本项目改用 5175，开发）/ 构建后由后端托管静态文件。

---

## 3. 数据源层（重点：风控与多源切换）

这是整个系统的「地基」，必须先解决东财风控问题。

### 3.1 统一接口

```python
class DataSourceInterface(ABC):
    name: str

    @abstractmethod
    async def get_kline(self, symbol: str, period: str, count: int) -> pd.DataFrame: ...

    @abstractmethod
    async def get_realtime_quote(self, symbols: list[str]) -> list[Quote]: ...

    @abstractmethod
    async def get_stock_list(self, market: str) -> list[StockInfo]: ...

    @abstractmethod
    async def health_check(self) -> bool: ...
```

返回的 K 线 DataFrame 统一列：`[date, open, high, low, close, volume, amount]`，周期统一映射：`1m/5m/15m/30m/60m/daily/weekly`。

### 3.2 数据源管理器（自动切换）

`DataSourceManager` 维护一个**优先级队列**，带健康分：

- 每源一个 `HealthState`：连续失败次数、最近成功时间、平均延迟、是否熔断。
- 健康检查：每 60s 后台探活一次；调用失败累计 ≥ 阈值则该源「熔断」一段时间。
- 取数时：按优先级 + 健康分排序，依次尝试，首个成功即返回，并把该源置顶「近期偏好」。
- 默认优先级：**mootdx（本地协议，最稳）> 腾讯财经 > 东财 push2his > AKShare**。

### 3.3 东财风控专项（按你给的注意事项落地）

针对 `push2his.eastmoney.com` / `push2.eastmoney.com` 的连接级间歇风控：

| 措施 | 实现 |
|------|------|
| **降频** | 东财源专用令牌桶限速，请求间隔 ≥ 1.5s（可配），默认 2s |
| **串行** | 东财源 `MAX_WORKERS=1`，单队列顺序执行，绝不并发 |
| **重试退避** | 指数 backoff：1s → 2s → 4s，最多 3 次；区分「网络错」与「风控错」，风控错直接切源不重试 |
| **换出口 IP** | 配置项支持 `proxy_pool`（HTTP 代理列表轮换）；文档提示手机热点/云服务器更稳 |
| **UA + NID** | 随机 User-Agent 池；开启 `ENABLE_EASTMONEY_PATCH` 时注入 NID 令牌（从一次首页请求获取 cookie） |
| **熔断** | 连续 3 次风控失败 → 该源熔断 10 分钟，期间全部走备用源 |

**关键业务绝不只绑东财**：选股扫描、信号生成的 K 线取数一律走 `DataSourceManager`，由它决定用哪个源；东财只作为「候选之一」。

### 3.4 缓存层

- K 线缓存：SQLite 表 `kline_cache(symbol, period, date, ohlcv)`，取数前先查本地，缺失部分才回源。
- 实时行情：内存 LRU（TTL 5s），避免短时间重复打。
- 盘后批量预热：每日收盘后跑一次全量 K 线更新，白天只取增量。

---

## 4. 核心模块设计

### 4.1 全局参数配置 `core/config.py`

集中管理所有可调参数，前端可读写，热生效。分组：

```yaml
趋势:
  ma_short: 10          # 短期均线
  ma_mid: 20
  ma_long: 60
  trend_filter: ADX     # 趋势过滤指标
  adx_threshold: 25     # ADX>25 视为有趋势

动量:
  roc_period: 12
  rsi_period: 14
  rsi_overbought: 70
  rsi_oversold: 30
  macd_fast: 12
  macd_slow: 26
  macd_signal: 9

量能:
  volume_ma: 20
  volume_ratio_threshold: 1.5   # 量比>1.5 视为放量

风控:
  daily_loss_limit_pct: 3.0     # 日内亏损熔断
  consecutive_loss_limit: 3     # 连续亏损次数触发降仓
  max_drawdown_pct: 10.0        # 最大回撤熔断
  single_position_pct: 25.0     # 单票最大仓位
  total_position_pct: 80.0      # 总仓位上限
  stop_loss_pct: 5.0            # 个股止损
  trailing_stop_pct: 8.0        # 移动止损

仓位:
  strategy: pyramid             # pyramid | kelly | fixed
  pyramid_ratios: [0.5, 0.3, 0.2]  # 首仓/加一/加二
  take_profit_levels: [1.03, 1.06, 1.10]  # 分批止盈
  kelly_fraction: 0.5           # 凯利折扣系数(防过赌)

做T:
  enable: true
  min_swing_pct: 1.5            # 最小日内波动阈值
  support_lookback: 5           # 支撑位回看天数
  t_position_ratio: 0.3         # 做T动用仓位比例

评分权重:
  timing: 0.25        # 进场时机
  position: 0.20      # 仓位合理性
  stop: 0.20          # 止损纪律
  profit: 0.20        # 止盈执行
  discipline: 0.15    # 计划执行度

数据源:
  priority: [mootdx, tencent, eastmoney, akshare]
  eastmoney:
    interval_sec: 2.0
    max_workers: 1
    retry: 3
    enable_patch: true
  proxy_pool: []

llm:
  provider: openai_compatible   # 兼容 openai 协议
  base_url: https://api.openai.com/v1
  api_key: ""
  model: gpt-4o-mini
  temperature: 0.3
  max_tokens: 2000
  timeout_sec: 60
```

存储：`config` 表单行 JSON，启动加载，修改经 API 写回 + 通知各模块刷新内存配置。

### 4.2 技术指标计算 `core/indicators/`

全部纯函数，输入 DataFrame → 输出带新列的 DataFrame，便于回测与单测。

| 指标 | 函数 | 用途 |
|------|------|------|
| MA/EMA | `ma(df, n)` / `ema(df, n)` | 趋势方向、金叉死叉 |
| MACD | `macd(df, fast, slow, signal)` | 动量拐点 |
| RSI | `rsi(df, n)` | 超买超卖 |
| ADX | `adx(df, n)` | 趋势强度过滤 |
| ROC | `roc(df, n)` | 动量速率 |
| 布林带 | `bollinger(df, n, k)` | 波动区间/做T支撑 |
| 唐奇安通道 | `donchian(df, n)` | 突破信号 |
| 量比 | `volume_ratio(df, n)` | 量能确认 |
| ATR | `atr(df, n)` | 止损距离/波动 |

### 4.3 信号生成 `core/signals/`

`SignalEngine` 产出结构化信号，每个信号带：`type / symbol / direction / strength(0-100) / reason(人话) / indicators_snapshot`。

**入场信号（BUY_FIRST）**：趋势 + 动量 + 量能三共振
- ADX > 阈值（有趋势）且 MA 短>中>长（多头排列）
- MACD 金叉或 ROC 由负转正
- 量比 > 阈值（放量确认）
- 三项各占权重，加权得分 ≥ 入场线才出信号

**加仓信号（BUY_ADD）**：趋势延续
- 已持仓且浮盈
- 回踩 MA 中轨不破 + 动量未死叉
- 触发金字塔下一档

**减仓信号（SELL_REDUCE）**：动量衰减
- 顶背离 / RSI 顶背离
- 触及分批止盈第一/二档
- 量能背离（价升量缩）

**止损信号（SELL_STOP）**：趋势破坏
- 跌破止损线（成本-止损%）或移动止损线
- ADX 掉头 + MA 短穿中
- 触发风控熔断时个股强制止损

**做T信号（T_BUY / T_SELL）**：日内波段
- 前提：该票已持仓（做T需底仓）
- T_BUY：日内回踩布林下轨/日内支撑 + 量缩
- T_SELL：日内冲布林上轨/日内阻力 + 量缩
- 日内必须平掉做T部分（不留隔夜增量）

### 4.4 仓位管理 `core/position/`

`PositionManager` 管理虚拟持仓（不接实盘，记录意图）。

- **金字塔加仓**：按 `pyramid_ratios` 分批，每次加仓后重算成本，加仓价必须高于上次（顺向加）。
- **分批止盈**：触及 `take_profit_levels` 各档各减一部分，剩余仓位用移动止损保护。
- **凯利公式**：`f = p - (1-p)/b`，p 用历史胜率估计，b 用历史盈亏比估计，再乘 `kelly_fraction` 折扣防过赌；输出建议仓位%。
- **仓位状态机**：`空仓 → 首仓 → 加仓 → 减仓 → 清仓`，每次状态转移落库。

### 4.5 选股器 `core/screener/`

`StockScreener` 全市场扫描 → 多因子打分 → 排名。

- **扫描池**：全 A（沪深），可按市值/行业/ST 过滤。
- **三因子评分**（与入场信号同源）：
  - 趋势分：ADX + 多头排列程度（0-40）
  - 动量分：ROC + RSI 位置 + MACD 柱状（0-40）
  - 量能分：量比 + 量价配合度（0-20）
- **过滤**：剔除 ST、停牌、流动性不足（日均成交额阈值）。
- **输出**：Top N 排名表，含每项得分、总分、人话理由、建议关注度。
- **调度**：盘后定时全量扫（约 5000 只，串行取数 + 缓存，控制总耗时）；盘中对自选股增量刷新。

### 4.6 交易计划生成 `core/plan/`

把信号 + 仓位 + 风控综合成**一份人话操作指引**，这是系统最核心的「交付物」。

输出结构：

```
【交易计划 2026-08-07】
标的状态：宁德时代(300750) 已持仓 浮盈+4.2%
当前信号：加仓信号(强度78) — 回踩20日均线企稳,MACD未死叉
建议操作：加仓 30% 仓位(金字塔第二档)
触发价位：现价附近 ±0.5%
止损价位：215.0 (成本下移3%)
止盈计划：第一档 235.0 减30% | 第二档 245.0 减30% | 余仓移动止损8%
风控检查：今日已亏损 -1.2%(未熔断) | 总仓位65%(未超限) ✓
做T提示：日内支撑218.5/阻力226.0,波动率1.8%(满足做T阈值)
纪律提醒：加仓后总仓位勿超80%
```

人话模板化生成，避免生硬指标罗列。

### 4.7 风控模块 `core/risk/`

`RiskManager` 在每个信号产生前做「闸门检查」，不通过直接拦截。

- **日亏损熔断**：当日累计浮动+实现亏损达 `daily_loss_limit_pct` → 停止一切买入信号（仅允许减仓/止损）。
- **连续亏损降仓**：连续 N 笔亏损 → 后续新仓位上限自动降至原值的 50%，直到扭亏。
- **回撤控制**：组合回撤达 `max_drawdown_pct` → 触发「防守模式」，仓位上限砍半。
- **单票/总仓位限制**：硬性上限。
- **状态持久化**：熔断/防守模式状态写库，重启不丢，需手动 `reset`。

### 4.8 交易日志 `core/logger/`

- **双写**：每笔信号/计划/手动回填的成交都写 SQLite `trades` 表 + 追加 CSV。
- CSV 字段：`time, symbol, name, action, price, qty, amount, reason, signal_strength, plan_id, pnl, score, note`
- 支持：按日期/标的/动作筛选、导出 CSV/Excel、批量导入历史成交。
- 手动回填：前端表单录入实际成交价/量，系统据此更新持仓与盈亏。

### 4.9 历史回顾 `core/stats/`

| 功能 | 说明 |
|------|------|
| 周统计 | 本周交易笔数/胜率/盈亏/平均持仓天数 |
| 月统计 | 月度收益/最大回撤/夏普/盈亏比 |
| 盈亏曲线 | 累计净值曲线（前端 ECharts） |
| 月度热力图 | 按月×日的盈亏热力图（红盈绿亏，符合中文习惯） |
| 信号分布饼图 | 入场/加仓/减仓/止损/做T 各类信号占比 |
| 个股贡献 | 哪些票贡献最多盈利/亏损 |

### 4.10 AI 复盘引擎 `core/ai_review/`

分层复盘，规则先行，LLM 兜底：

1. **规则诊断**（确定性）：
   - 是否违反止损纪律（信号止损但未执行）
   - 是否追高（买入价偏离当日均价）
   - 是否频繁交易（同标的日内多次）
   - 是否逆势操作（趋势向下仍买入）
2. **模式识别**（统计性）：
   - 聚类亏损交易的共同特征（时段/市值/板块）
   - 胜率随仓位变化的曲线
3. **LLM 深度复盘**（可配/可关）：
   - 注入：近 N 天交易记录 + 对应行情 + 指标快照 + 规则诊断结果
   - Prompt 模板要求 LLM 输出：问题归因 / 可执行改进建议 / 本周纪律评分
   - 兼容 OpenAI 协议，可接 DeepSeek/通义/Kimi/本地 Ollama
   - 结果落库，可查历史复盘
4. **改进建议追踪**：每条建议打 `已采纳/未采纳` 标记，后续统计采纳率与收益改善。

### 4.11 交易评分 `core/scoring/`

- **单笔评分**（0-100）：按 `评分权重` 多维打分
  - 时机分：进场点相对信号价的偏离
  - 仓位分：实际仓位 vs 建议仓位
  - 止损分：是否设止损、是否执行
  - 止盈分：是否按计划分批
  - 纪律分：是否按信号操作、有无情绪化交易
- **时段评分**：周/月综合分 = 胜率×0.3 + 盈亏比×0.3 + 纪律×0.2 + 收益×0.2
- **系统健康度**：信号质量（命中率）、风控触发频率、纪律执行率、夏普比率，仪表盘展示。

### 4.12 终端可视化 `cli/`

用 `rich` 做一个备用的命令行面板（前端不可用时的兜底）：
- 实时自选股表格（涨红跌绿）
- 信号提示卡片
- 风控状态面板
- 进度条（选股扫描时）

---

## 5. 数据库设计（SQLite）

```
stocks        股票代码/名称/市场/行业
config        单行 JSON 全局配置
kline_cache   K线缓存 (symbol, period, date, ohlcv)
positions     持仓快照 (symbol, qty, cost, status, updated_at)
signals       信号记录 (time, symbol, type, strength, reason, indicators_json)
plans         交易计划 (id, time, symbol, content, action, status)
trades        成交记录 (time, symbol, action, price, qty, pnl, score, plan_id, note)
risk_state    风控状态 (熔断标志, 防守模式, 连亏次数, updated_at)
ai_reviews    AI复盘记录 (time, range, content, suggestions_json, model)
scores        评分 (scope, scope_id, dims_json, total, created_at)
daily_stats   每日净值/回撤快照 (date, equity, drawdown)
```

索引：`trades(time)`, `trades(symbol)`, `signals(symbol,time)`, `kline_cache(symbol,period,date)`。

---

## 6. 接口设计（FastAPI，端口 8002）

统一前缀 `/api`，统一响应 `{code, msg, data}`。所有写操作需二次确认参数。

### 6.1 系统/配置
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查（容器 HEALTHCHECK 用，含数据源状态） |
| GET | `/api/config` | 读取全局配置 |
| PUT | `/api/config` | 更新配置（热生效） |
| GET | `/api/data-sources/status` | 各数据源健康状态/延迟/熔断 |
| POST | `/api/data-sources/test/{name}` | 手动测试某数据源 |

### 6.2 行情
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/quote/{symbol}` | 实时行情 |
| GET | `/api/kline/{symbol}?period=daily&count=120` | K线（优先缓存） |
| GET | `/api/indicators/{symbol}` | 计算并返回该票全部指标 |
| WS | `/ws/quote?symbols=...` | 实时行情推送（可选，5s 轮询源） |

### 6.3 选股
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/screener/run` | 触发全市场扫描（异步，返回任务id） |
| GET | `/api/screener/result?task_id=` | 获取扫描结果（Top N） |
| GET | `/api/screener/result/latest` | 最近一次结果 |
| GET | `/api/watchlist` | 自选股列表 |
| POST | `/api/watchlist` | 添加自选 |
| DELETE | `/api/watchlist/{symbol}` | 移除自选 |

### 6.4 信号与计划
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/signals?symbol=&limit=` | 信号列表 |
| GET | `/api/signals/{symbol}` | 某票最新信号 |
| POST | `/api/plan/generate` | 为指定票生成交易计划 |
| GET | `/api/plan/current` | 当前未完成计划 |
| GET | `/api/plan/{id}` | 计划详情 |
| PUT | `/api/plan/{id}/status` | 标记计划 已执行/已忽略 |

### 6.5 持仓
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/positions` | 当前持仓（含浮盈） |
| POST | `/api/positions` | 手动录入/调整持仓 |
| GET | `/api/positions/{symbol}` | 单票持仓详情+历史 |

### 6.6 风控
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/risk/status` | 风控状态（熔断/防守/连亏/仓位） |
| POST | `/api/risk/reset` | 重置熔断/防守（需确认） |

### 6.7 交易日志
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/trades?start=&end=&symbol=&action=` | 查询成交 |
| POST | `/api/trades` | 录入成交（手动回填） |
| GET | `/api/trades/export?format=csv|xlsx` | 导出 |

### 6.8 历史回顾
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stats/weekly?week=` | 周统计 |
| GET | `/api/stats/monthly?month=` | 月统计 |
| GET | `/api/stats/equity-curve?start=&end=` | 盈亏曲线数据 |
| GET | `/api/stats/heatmap?year=` | 月度热力图数据 |
| GET | `/api/stats/signal-distribution` | 信号分布饼图数据 |

### 6.9 AI 复盘
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ai-review/run` | 触发复盘（指定范围/标的，异步） |
| GET | `/api/ai-review/result?task_id=` | 复盘结果 |
| GET | `/api/ai-review/history` | 历史复盘列表 |
| GET | `/api/ai-review/config` | LLM 配置（key 脱敏） |
| PUT | `/api/ai-review/config` | 更新 LLM 配置 |

### 6.10 评分
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/score/trade/{id}` | 单笔评分 |
| GET | `/api/score/period?range=week|month` | 时段评分 |
| GET | `/api/score/health` | 系统健康度仪表盘数据 |

---

## 7. 前端设计（Vite + React, 5175）

### 页面结构

```
布局：左侧导航 + 顶部状态条(风控状态/数据源状态/时间) + 主内容区

1. 仪表盘 Dashboard
   - 风控状态卡片 / 系统健康度 / 今日信号数 / 持仓盈亏
   - 盈亏曲线缩略
2. 选股 Screener
   - 触发扫描按钮 + 进度
   - 排名表(虚拟列表) + 点选加入自选
3. 自选与持仓 Watchlist & Positions
   - 自选股实时表(涨红跌绿) + K线(lightweight-charts)
   - 持仓明细 + 浮盈
4. 信号中心 Signals
   - 最新信号流 + 过滤
5. 交易计划 Plans
   - 当前计划卡片(人话指引) + 历史
   - 一键标记已执行/录入成交
6. 交易日志 Trades
   - 表格 + 筛选 + 导出 + 录入
7. 历史回顾 Review
   - 盈亏曲线 / 月度热力图 / 信号饼图(ECharts)
   - 周/月统计卡片
8. AI复盘 AI Review
   - 触发复盘 + 结果展示 + 历史列表
   - LLM 配置面板
9. 设置 Settings
   - 全局参数配置(分组表单) / 数据源配置 / 评分权重
```

### 性能要点

- **虚拟列表**：选股/自选/日志千行级表格一律虚拟滚动。
- **K线图**：lightweight-charts 而非 ECharts 画 K 线（性能差一个量级）。
- **数据缓存**：React Query，行情 5s 轮询、K线按需、配置长缓存。
- **按需加载**：页面级 React.lazy + Suspense，复盘/设置等重页延迟加载。
- **WebSocket 行情**：自选股页面才订阅，离开即断。
- **防抖**：筛选/搜索输入 300ms 防抖。
- **涨跌色**：全局常量 `UP=#ef4142`(红) `DOWN=#22a55b`(绿)，集中管理。

---

## 8. 目录结构

```
trading-system/
├── backend/
│   ├── app/
│   │   ├── main.py                  # FastAPI 入口(含 /api/health、静态托管)
│   │   ├── api/
│   │   │   ├── config.py  screener.py  quote.py  signals.py
│   │   │   ├── plans.py  positions.py  risk.py  trades.py
│   │   │   ├── stats.py  ai_review.py  scores.py
│   │   │   └── deps.py              # 依赖注入
│   │   ├── core/
│   │   │   ├── config.py            # 配置中心(env 覆盖优先)
│   │   │   ├── datasource/
│   │   │   │   ├── base.py  manager.py  mootdx_src.py
│   │   │   │   ├── tencent_src.py  eastmoney_src.py  akshare_src.py
│   │   │   ├── indicators/          # 指标纯函数
│   │   │   ├── signals/             # 信号引擎
│   │   │   ├── position/            # 仓位管理
│   │   │   ├── screener/            # 选股器
│   │   │   ├── risk/                # 风控
│   │   │   ├── plan/                # 计划生成
│   │   │   ├── scoring/             # 评分
│   │   │   ├── ai_review/           # AI复盘
│   │   │   └── stats/               # 统计
│   │   ├── models/                  # SQLModel 表
│   │   ├── services/                # 业务编排
│   │   ├── scheduler.py             # APScheduler 定时任务
│   │   └── db.py
│   ├── cli/                         # rich 终端面板
│   ├── data/                        # SQLite + CSV(容器挂载 /app/data)
│   ├── tests/
│   ├── requirements.txt
│   └── requirements-dev.txt         # ruff/pytest/mypy
├── frontend/
│   ├── src/
│   │   ├── pages/   components/   api/   store/   utils/   const/
│   │   └── main.tsx
│   ├── vite.config.ts               # build 输出 dist/ 供后端托管
│   └── package.json
├── .github/
│   ├── workflows/
│   │   ├── ci.yml                   # push/PR: 后端lint+test + 前端build
│   │   └── docker-publish.yml       # tag v*: 镜像推送 GHCR
│   ├── ISSUE_TEMPLATE/
│   └── PULL_REQUEST_TEMPLATE.md
├── docs/
├── Dockerfile                       # 多阶段: node 构建前端 → python 运行时
├── docker-compose.yml               # 生产: 单容器, 端口 8002
├── docker-compose.dev.yml           # 开发: backend 热重载 + vite HMR
├── Makefile                         # make dev / make build / make up
├── pyproject.toml                   # 后端依赖与工具链
├── .dockerignore
├── .env.example                     # LLM key / TZ / 数据源开关
├── .gitignore
├── LICENSE                          # MIT
└── README.md
```

---

## 9. Docker 部署

### 9.1 两种部署模式

| 模式 | 文件 | 结构 | 用途 |
|------|------|------|------|
| 开发 | `docker-compose.dev.yml` | backend(8002, 挂载源码热重载) + frontend(5175, Vite HMR) | 日常开发 |
| 生产 | `docker-compose.yml` | 单容器 app(8002)，前端构建产物由 FastAPI 托管 | 一键部署 |

生产模式单容器即可（个人系统无需 nginx）；如需上公网/HTTPS，可前置 nginx 反代（文档给示例）。

### 9.2 Dockerfile（多阶段构建）

```dockerfile
# 阶段1: 构建前端
FROM node:22-alpine AS frontend-build
WORKDIR /build/frontend
COPY frontend/package*.json ./
RUN npm ci
COPY frontend/ ./
RUN npm run build

# 阶段2: 运行时
FROM python:3.11-slim AS runtime
ENV TZ=Asia/Shanghai \
    LANG=C.UTF-8 \
    PYTHONUNBUFFERED=1
WORKDIR /app
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY backend/ ./backend/
COPY --from=frontend-build /build/frontend/dist/ ./frontend/dist/
VOLUME ["/app/data"]
EXPOSE 8002
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD ["python", "-c", "import urllib.request;urllib.request.urlopen('http://127.0.0.1:8002/api/health')"]
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8002"]
```

要点：
- **多阶段**：镜像里只有前端产物，没有 node_modules；`.dockerignore` 排除 `data/`、`.git`、`node_modules`、`__pycache__`、`.env`
- **TZ=Asia/Shanghai 必须设**：A股交易时段判断依赖时区
- **端口固定 8002**：与本地开发一致，不引入额外映射层
- `requirements.txt` 先 COPY（依赖层缓存），代码后 COPY（改码不重装依赖）

### 9.3 docker-compose.yml（生产）

```yaml
services:
  app:
    build: .
    container_name: momentum-trader
    ports:
      - "8002:8002"
    volumes:
      - ./data:/app/data        # SQLite + CSV + K线缓存持久化
    env_file:
      - .env                    # LLM key / 数据源开关 / TZ
    restart: unless-stopped
```

### 9.4 数据持久化与备份

- `/app/data` 挂载宿主目录：`trading.db`、`trades.csv`、`kline_cache`、`config`
- 备份一行命令：
  ```bash
  docker run --rm -v "$PWD/data":/data -v "$PWD":/backup alpine \
    tar czf /backup/data-$(date +%F).tar.gz /data
  ```
- 升级容器（`docker compose pull && up -d`）不丢数据，因为数据在卷上

### 9.5 敏感配置（.env）

- `.env.example` 提交仓库；`.env` 本地创建且被 gitignore
- LLM api_key 等只经环境变量注入，config 中心读取顺序：**env > 数据库配置 > 默认值**
- 绝不把 key 写进数据库/CSV/备份

### 9.6 定时任务与容器

- APScheduler 随 uvicorn 同进程运行（个人系统够用）
- 进阶可选：拆分 `worker` 容器跑选股/复盘任务（compose 加一个 service）

---

## 10. GitHub 项目管理

### 10.1 仓库初始化清单

- [ ] `git init` + `main` 分支（受保护，PR 合入）
- [ ] `.gitignore`（见下）+ `.env.example` + `LICENSE`(MIT) + `README.md`
- [ ] 首提交即工程完整，不留敏感文件

### 10.2 .gitignore 要点

```
.env
data/
*.db
*.csv
__pycache__/
*.pyc
.venv/
node_modules/
frontend/dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
```

> 若想演示默认配置，可放 `data/.gitkeep` + `data/README.md` 说明，数据文件不入库。

### 10.3 CI（.github/workflows/ci.yml）

- 触发：`push` + `pull_request`
- **job backend**：setup-python 3.11（cache pip）→ 安装 `requirements-dev.txt` → `ruff check` + `pytest`
- **job frontend**：setup-node 22（cache npm）→ `npm ci` → `npm run build`
- **重要**：测试用 fixture 行情数据（`tests/fixtures/`），CI 不打真实上游，避免被东财限流、也保证 CI 确定性

### 10.4 镜像发布（.github/workflows/docker-publish.yml）

- 触发：`v*` tag 或 `workflow_dispatch`
- 流程：`docker/setup-buildx-action` → `docker/login-action`(GHCR, `GITHUB_TOKEN`) → `build-push-action` 推送 `ghcr.io/<owner>/momentum-trader:<tag>` + `:latest`
- 用户部署：`docker pull ghcr.io/<owner>/momentum-trader:latest && docker compose up -d`

### 10.5 版本与发布约定

- **SemVer**：`v0.1.0`(一期) → `v0.2.0`(二期) → `v0.3.0`(三期) → `v1.0.0`(四期+发布清单过检)
- **Conventional Commits**（`feat/fix/docs/chore/refactor`），可配 release-please 自动生成 CHANGELOG
- 每期结束打 tag → 自动触发镜像发布

### 10.6 README 结构

```
# Momentum Trader 动量趋势交易系统
徽章: CI | GHCR | License
一句话简介 + 功能特性(配截图占位)
快速开始: ① Docker 一键(推荐) ② 本地开发(backend+frontend)
数据源与东财限流声明(重要!)   ← 开源用户最容易踩的坑
配置说明: .env / 全局参数
目录结构
免责声明: 仅供学习研究, 不构成投资建议, 投资有风险
License: MIT
```

### 10.7 安全基线

- 真实 key 永不进仓库；Actions 用 **GitHub Secrets**
- 若误提交敏感信息：立即轮换 key + `git filter-repo` 清理历史
- 数据（持仓/成交）不进公开仓库，除非明确做脱敏示例

---

## 11. 实施路线图

分四期，每期可独立跑通、可发布一个版本：

**一期 · 地基（数据通路 + 工程骨架）→ tag v0.1.0**
- 项目骨架 + git 初始化 + `.gitignore/.env.example/LICENSE` + 目录结构
- 数据源管理器 + mootdx/腾讯/东财三源 + 东财风控策略（降频/串行/退避/UA+NID/熔断/proxy_pool）
- 统一接口 + 缓存 + 健康检查 + 自动切换
- 指标库（全套纯函数）+ 配置中心 + SQLite 建表
- 基础 API（`/api/health`、行情/K线/指标）+ rich 面板
- `docker-compose.dev.yml` 双容器跑通 + CI（ruff+pytest+前端 build）

**二期 · 决策大脑 → tag v0.2.0**
- 信号引擎（五类信号）+ 仓位管理（金字塔/分批止盈/凯利）
- 风控模块（三道熔断）+ 选股器 + 交易计划生成
- 信号/计划/风控 API

**三期 · 闭环（记录与复盘）→ tag v0.3.0**
- 交易日志（双写 + 查询 + 导出）+ 持仓管理 + 手动回填
- 历史回顾（统计/曲线/热力图/饼图）+ 交易评分（单笔/时段/健康度）

**四期 · AI 与前端 + 发布 → tag v1.0.0**
- AI 复盘引擎（规则诊断 + LLM 对接）
- 前端全部页面 + 性能优化 + 联调
- 生产 `Dockerfile`(多阶段) + `docker-compose.yml` + 健康检查
- `docker-publish.yml`(GHCR) + README 完整化 + 截图 + 免责声明

**v1.0.0 发布清单**
- [ ] 全新环境 `docker compose up` 可一键跑通
- [ ] CI 全绿、GHCR 镜像可构建可拉取
- [ ] 数据卷备份/恢复演练通过
- [ ] 确认无 key/数据泄漏进仓库
- [ ] README 含数据源限流声明与免责声明

---

## 12. 风险与对策

| 风险 | 对策 |
|------|------|
| 东财风控导致数据中断 | 三源自动切换 + 东财降频串行 + 熔断；mootdx 作为稳定主源 |
| 选股全市场扫描慢 | 盘后批量 + 缓存 + 串行限速；盘中只刷自选 |
| LLM 复盘成本/不可用 | 规则诊断不依赖 LLM；LLM 可关可换；超时降级 |
| 回测过拟合 | 评分权重默认值，可调；提供样本外验证 |
| 数据源 API 变动 | 统一接口隔离，单源改动不影响业务 |
| Docker 镜像体积大/构建慢 | 多阶段构建 + `.dockerignore` 排除 data/node_modules；依赖层缓存 |
| CI 打真实数据源被限流 | CI 用 fixture 行情数据，不请求真实上游 |
| 容器时区错乱导致交易时段判断错误 | 强制 `TZ=Asia/Shanghai`，健康检查含时间自检 |
| LLM key 泄漏进仓库 | `.env` gitignore + GitHub Secrets + 泄漏立即轮换 |

---

**下一步**：你确认这份方案后，我从一期「地基」开始落地——先建项目骨架（含 git/Docker/CI 工程化）+ 数据源管理器 + 指标库，跑通取数通路。
